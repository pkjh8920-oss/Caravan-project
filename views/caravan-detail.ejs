<%- include('layout_head') %>

<div class="bg-white rounded-lg shadow-lg overflow-hidden max-w-5xl mx-auto mt-6 border border-gray-200">
    <div class="relative h-96">
        <img src="<%= caravan.image %>" class="w-full h-full object-cover" alt="<%= caravan.name %>">
        <div class="absolute bottom-0 left-0 bg-gradient-to-t from-black to-transparent w-full p-6 text-white">
            <h1 class="text-4xl font-bold mb-2"><%= caravan.name %></h1>
            <p class="text-lg opacity-90 flex items-center gap-2">
                📍 <%= caravan.location %>
            </p>
        </div>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-3 gap-8 p-8">
        <div class="md:col-span-2 space-y-8">
            <div>
                <h3 class="text-xl font-bold mb-3 border-b pb-2">호스트</h3>
                <div class="flex items-center gap-3">
                    <div class="bg-gray-200 rounded-full w-12 h-12 flex items-center justify-center font-bold text-gray-600">
                        <%= caravan.host.name[0] %>
                    </div>
                    <div>
                        <p class="font-bold"><%= caravan.host.name %></p>
                        <p class="text-sm text-gray-500">신뢰도 ⭐ <%= caravan.host.reliability.toFixed(1) %></p>
                    </div>
                </div>
            </div>

            <div>
                <h3 class="text-xl font-bold mb-3 border-b pb-2">편의시설</h3>
                <div class="flex flex-wrap gap-2">
                    <% if(caravan.amenities) { %>
                        <% caravan.amenities.split(',').forEach(a => { %>
                            <span class="bg-blue-50 text-blue-700 px-4 py-2 rounded-lg text-sm font-semibold">
                                <%= a.trim() %>
                            </span>
                        <% }) %>
                    <% } else { %>
                        <span class="text-gray-400">등록된 편의시설 정보가 없습니다.</span>
                    <% } %>
                </div>
            </div>

            <div>
                <h3 class="text-xl font-bold mb-3 border-b pb-2">카라반 소개</h3>
                <p class="text-gray-700 leading-relaxed">
                    조용하고 아늑한 휴식을 즐길 수 있는 최고의 카라반입니다. 
                    <%= caravan.capacity %>명까지 수용 가능하며, 자연 속에서 잊지 못할 추억을 만들어보세요.
                </p>
            </div>

            <div>
                <h3 class="text-xl font-bold mb-3 border-b pb-2">위치 보기</h3>
                <div class="w-full h-64 bg-gray-100 rounded-xl overflow-hidden shadow-inner relative group">
                    <iframe 
                        width="100%" 
                        height="100%" 
                        style="border:0" 
                        loading="lazy" 
                        allowfullscreen
                        src="https://maps.google.com/maps?q=gangwon-do&t=&z=13&ie=UTF8&iwloc=&output=embed">
                    </iframe>
                    <div class="absolute inset-0 bg-transparent"></div>
                </div>
                <p class="text-xs text-gray-500 mt-2">* 정확한 위치는 예약 확정 후 게스트에게만 공개됩니다 (보안 정책).</p>
            </div>
        </div>

        <div class="md:col-span-1">
            <div class="bg-white border rounded-xl p-6 shadow-lg sticky top-4">
                <div class="flex justify-between items-end mb-6">
                    <span class="text-gray-500 font-medium">1박 요금</span>
                    <span class="text-3xl font-bold text-blue-600">₩<%= caravan.price.toLocaleString() %></span>
                </div>

                <form action="/book/<%= caravan.id %>" method="POST" class="space-y-4">
                    <div class="space-y-2">
                        <label class="block text-gray-700 font-bold text-sm">체크인</label>
                        <input type="date" name="startDate" required 
                               class="w-full border-gray-300 border p-3 rounded-lg focus:ring-2 focus:ring-blue-500 outline-none">
                    </div>
                    <div class="space-y-2">
                        <label class="block text-gray-700 font-bold text-sm">체크아웃</label>
                        <input type="date" name="endDate" required 
                               class="w-full border-gray-300 border p-3 rounded-lg focus:ring-2 focus:ring-blue-500 outline-none">
                    </div>

                    <hr class="border-dashed my-4">

                    <button class="w-full bg-blue-600 text-white py-4 rounded-xl font-bold text-lg hover:bg-blue-700 transition shadow-md hover:shadow-lg transform hover:-translate-y-0.5">
                        예약 요청하기
                    </button>
                    
                    <p class="text-xs text-center text-gray-500 mt-2">
                        🔒 예약 확정 전에는 요금이 청구되지 않습니다.
                    </p>
                </form>

                <button onclick="shareLink()" class="mt-3 w-full border border-gray-300 text-gray-600 py-3 rounded-xl font-bold hover:bg-gray-50 transition flex justify-center items-center gap-2">
                    🔗 링크 공유하기
                </button>
            </div>
        </div>
    </div>
</div>

<div class="h-20"></div>

<script>
function shareLink() {
    const url = window.location.href;
    navigator.clipboard.writeText(url).then(() => {
        alert('링크가 복사되었습니다! 친구에게 공유해보세요 🚐');
    });
}
</script>

</body>
</html>