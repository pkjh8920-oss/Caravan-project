<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CaravanShare</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-50 text-gray-800">
    
    <nav class="bg-white shadow sticky top-0 z-50">
        <div class="container mx-auto px-4 py-3 flex justify-between items-center">
            <a href="/" class="text-2xl font-bold text-blue-600 flex items-center gap-2">
                🚐 CaravanShare
            </a>

            <div class="flex items-center gap-4">
                <% if (user) { %>
                    <span class="text-gray-600 font-medium hidden md:inline">
                        👋 <%= user.name %>님
                        <% if(user.role === 'HOST') { %> (호스트) <% } %>
                    </span>
                    <a href="/dashboard" class="text-gray-600 hover:text-blue-600 font-bold transition">
                        대시보드
                    </a>
                    <a href="/logout" class="bg-red-50 text-red-500 px-4 py-2 rounded-lg font-bold hover:bg-red-100 transition">
                        로그아웃
                    </a>
                <% } else { %>
                    <a href="/login" class="text-gray-600 hover:text-blue-600 font-bold transition">
                        로그인
                    </a>
                    <a href="/signup" class="bg-blue-600 text-white px-4 py-2 rounded-lg font-bold hover:bg-blue-700 transition shadow">
                        회원가입
                    </a>
                <% } %>
            </div>
        </div>
    </nav>

    <div class="container mx-auto p-4 min-h-screen">